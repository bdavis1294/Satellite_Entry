The Project requires .NET Core 3.1 and Visual Studio to build on Windows.

Requires MS SQL Server is installed.

Update the Config.json file to replace the <server>  entrie to connect to your SQL Server.

Update the Config.json file to replace <username> and <password> with login information to Space-Track.org to access the REST API.